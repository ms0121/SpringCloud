#端口名
server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider   # 微服务名称
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848    # 配置nacos的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'


